'use strict';

/**
 * high-charts-line-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::high-charts-line-list.high-charts-line-list');
